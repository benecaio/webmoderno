// Operadores aritmeticos são binarios (dois operandos)
// 


const [a, b, c, d] = [3, 5, 1, 15]

const soma = a + b + c + d

// Existem operadores pré-fixos, pós-fixos e e infixo: a++ (pós-fixo), ++a (pré-fixo) e a + b (infixo).

const subtracao = d - c

const multiplicacao = a * b

const divisao = d / b

const modulo = a % 2

console.log(soma, subtracao, multiplicacao, -divisao, modulo)
// Note que podemos usar um operador pra mudar a constante (no caso a constante divisão).
